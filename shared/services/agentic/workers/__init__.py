"""Agentic worker package.

This package contains one Python module per registered worker.
Each worker module documents its own contract so the runtime behavior can be
understood without having to reverse-engineer the workflow graph first.
"""
