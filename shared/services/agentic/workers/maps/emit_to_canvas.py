"""Worker: ``mindmap.emit_to_canvas.v1``.

Purpose:
- Open the final map markdown in the canvas and persist it to ``result``.
"""
from __future__ import annotations

from ._support import emit_to_canvas as run

WORKER_ID = "mindmap.emit_to_canvas.v1"
