# Optional dependencies for the minimal profile.
# Excludes AGPL/GPL packages; includes DOCX, ODT, HTML (markdownify), sentence-transformers.
python-docx>=1.1.0
markdownify>=0.11.6
odfpy>=1.4.1
sentence-transformers>=2.7.0
