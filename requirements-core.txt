# draft2craift — core runtime dependencies
# Install this file only if you intentionally want a lean setup.

PySide6>=6.6.0
networkx>=3.2

# GGUF inference backend (choose hardware variant if needed):
#   CPU only:          pip install llama-cpp-python
#   CUDA (NVIDIA):     CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python
#   Metal (Apple Si):  CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
llama-cpp-python>=0.2.57
